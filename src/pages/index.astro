---
import Base from '../layouts/Base.astro';
import Basketball from '../components/Basketball.astro';
import ScrollStack from '../components/ScrollStack.astro';
import { getEntry, getCollection } from 'astro:content';

const highlights    = await getEntry('highlights', 'home');
const testimonials  = await getCollection('testimonials');

// Ensure at least 6 cards for the carousel
const carouselItems = (() => {
  if (testimonials.length === 0) return [];
  if (testimonials.length >= 6) return testimonials;
  const repeated: typeof testimonials = [];
  while (repeated.length < 6) repeated.push(...testimonials);
  return repeated.slice(0, 6);
})();

// ── Company link map ──────────────────────────────────────────────────────────
const companyData: Record<string, { url: string; logo?: string }> = {
  'Ontario Legal Tech':                      { url: 'https://ontariomandatorymediation.my.canva.site/ontario-court-helper' },
  "Queen's COMPSA":                          { url: 'https://compsa.ca/', logo: 'compsa' },
  'DoneMaker':                               { url: 'https://donemaker.com', logo: 'zivko' },
  'UWaterloo':                               { url: 'https://uwaterloo.ca', logo: 'uwaterloo' },
  'QueensU':                                 { url: 'http://queensu.ca/', logo: 'queenu' },
  'University of Toronto Engineering Outreach': { url: 'https://outreach.engineering.utoronto.ca', logo: 'uoft2' },
  'TypeScript':                              { url: 'https://www.typescriptlang.org' },
  'Next.js':                                 { url: 'https://nextjs.org' },
  'React':                                   { url: 'https://react.dev' },
  'Flask':                                   { url: 'https://flask.palletsprojects.com' },
  'FastAPI':                                 { url: 'https://fastapi.tiangolo.com' },
  'TailwindCSS':                             { url: 'https://tailwindcss.com' },
  'Figma':                                   { url: 'https://www.figma.com' },
  'Python':                                  { url: 'https://www.python.org' },
  'PyTorch':                                 { url: 'https://pytorch.org' },
  'NumPy':                                   { url: 'https://numpy.org' },
  'Pandas':                                  { url: 'https://pandas.pydata.org' },
  'Jupyter docs tool':                       { url: 'https://github.com/Elrich-Chen/jupyterlab_docs_helper' },
  'MongoDB':                                 { url: 'https://www.mongodb.com' },
  'JavaScript':                              { url: 'https://developer.mozilla.org/en-US/docs/Web/JavaScript' },
  'PostgreSQL':                              { url: 'https://www.postgresql.org' },
  'SQLAlchemy':                              { url: 'https://www.sqlalchemy.org' },
  'JWT':                                     { url: 'https://jwt.io' },
  'Stanford Code In Place':                  { url: 'https://codeinplace.stanford.edu', logo: 'stanford' },
  "Queen's Sports Analytics Organization":  { url: 'https://www.instagram.com/p/DH4J-30SmkA/?img_index=6', logo: 'qsao' },
  'Chinese Lion Dancing':                    { url: 'https://www.chinadailyhk.com/hk/article/603804' },
  'Tech Plus UW':                            { url: 'https://www.techplusuw.com', logo: 'techplusuw' },
  'UW FinTech':                              { url: 'https://www.instagram.com/uwfintech/?hl=en', logo: 'fintech' },
  'UW Data Science':                         { url: 'https://www.uwdatascience.ca/', logo: 'uw_dsc' },
};

const logoOverrides: Record<string, string> = {
  'uw_cube': 'cube',
  'uw cube': 'cube',
};

function resolveLogoFileName(logoName: string) {
  const cleaned = String(logoName).trim().toLowerCase();
  return logoOverrides[cleaned] ?? cleaned.replace(/\s+/g, '-');
}

function formatStatsAndCompanies(text: string) {
  const statPattern = /(\d+(?:\.\d+)?(?:st|nd|rd|th|[KMB%+]\+?))/gi;
  let result = text.replace(statPattern, '<strong><em>$1</em></strong>');

  result = result.replace(
    /\[LINK:\s*([^\]]+?)\s*\](.+?)\[\/LINK\]/gi,
    '<a href="$1" class="company-link" target="_blank" rel="noopener noreferrer"><strong><em>$2</em></strong></a>'
  );

  result = result.replace(/\[IMAGE:\s*([^\]|]+?)(?:\|([^\]]+))?\s*\]/gi, (_, fileName, link) => {
    const safeName = String(fileName).trim().replace(/^\/+/, '');
    const src = safeName.startsWith('http') ? safeName : `/photos/elrich_photos/${safeName}`;
    if (link) {
      const href = String(link).trim();
      return `<a href="${href}" class="highlight-preview-link" target="_blank" rel="noopener noreferrer"><img src="${src}" alt="Preview" loading="lazy" class="highlight-preview" /></a>`;
    }
    return `<img src="${src}" alt="Preview" loading="lazy" class="highlight-preview" />`;
  });

  result = result.replace(/\[LOGO:([^\]]+)\]/gi, (_, logoName) => {
    const fileName = resolveLogoFileName(String(logoName).trim());
    return `<img src="/logos/${fileName}.png" alt="${logoName}" width="20" height="20" loading="lazy" class="inline-block align-middle mx-1 logo-placeholder" onerror="if(!this.dataset.fallback){this.dataset.fallback='1';this.src='/logos/${fileName}.svg';}else{this.style.display='none';}" />`;
  });

  const companyKeys = Object.keys(companyData).sort((a, b) => b.length - a.length);
  const companyMap  = new Map(companyKeys.map((k) => [k.toLowerCase(), k]));
  const companyPattern = new RegExp(
    `(${companyKeys.map((k) => k.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')).join('|')})`,
    'gi'
  );

  const tokens = result.split(/(<[^>]+>)/g);
  let insideAnchor = false;
  const rebuilt: string[] = [];

  for (const token of tokens) {
    if (/^<a\b/i.test(token))  { insideAnchor = true;  rebuilt.push(token); continue; }
    if (/^<\/a/i.test(token))  { insideAnchor = false; rebuilt.push(token); continue; }
    if (token.startsWith('<') || insideAnchor) { rebuilt.push(token); continue; }
    rebuilt.push(token.replace(companyPattern, (match) => {
      const key  = companyMap.get(match.toLowerCase());
      if (!key) return match;
      const data = companyData[key];
      return `<a href="${data.url}" target="_blank" rel="noopener" class="company-link"><strong><em>${match}</em></strong></a>`;
    }));
  }
  result = rebuilt.join('');
  result = result.replace(/View project\s*→/gi, '<a href="/projects" class="view-project-link">View project →</a>');
  return result;
}

// Contact links used in both connect sections
const contactLinks = [
  { href: 'mailto:elrichchen123@gmail.com', label: 'Email',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M4 5h16a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2zm0 2v.01L12 13l8-5.99V7H4z"/></svg>` },
  { href: 'https://github.com/Elrich-Chen', label: 'GitHub',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>` },
  { href: 'https://linkedin.com/in/elrichchen', label: 'LinkedIn',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 23.2 24 22.222 0h.003z"/></svg>` },
  { href: 'https://x.com/ChenElrich_', label: 'X',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>` },
  { href: 'https://drive.google.com/drive/folders/1JB5jWDtohn4ddSp5ro-jeZshZw2Q-5lc?usp=share_link', label: 'Resume',
    icon: `<svg viewBox="0 0 24 24" fill="currentColor"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6zm4 18H6V4h7v5h5v11zM8 15h8v2H8v-2zm0-4h8v2H8v-2zm0-4h5v2H8V7z"/></svg>` },
];
---

<Base title="Elrich Chen" description="CS @ UWaterloo | AI automations for DoneMaker | Aspiring Backend Engineer" fullscreen={true}>


  <!-- Fixed basketball canvas — sits at z:15 for hero, drops to z:4 for content sections -->
  <Basketball />

  <!-- ── HERO ──────────────────────────────────────────────────────────────── -->
  <section id="section-hero" class="scroll-section hero-section">
    <div class="hero-content">
      <h1 class="hero-name">Elrich Chen</h1>
      <div class="hero-tagline">
        <a href="https://uwaterloo.ca/future-students/programs/computer-science" target="_blank" rel="noopener noreferrer" class="tagline-link">CS @ UWaterloo</a>
        <span class="tagline-dot">·</span>
        <span class="tagline-plain">Builder</span>
        <span class="tagline-dot">·</span>
        <a href="https://www.ycombinator.com/companies/medme-health" target="_blank" rel="noopener noreferrer" class="tagline-link">SWE @ MedMe Health (YC W21)</a>
      </div>
      <div class="hero-scroll-hint" aria-hidden="true">
        <span class="hint-label">scroll</span>
        <span class="hint-arrow">↓</span>
      </div>
    </div>
  </section>

  <!-- ── BODY: stacked transparent cards ──────────────────────────────────── -->
  <ScrollStack>

    {highlights?.data?.what_makes_me_different?.length > 0 && (
      <div class="scroll-stack-card" id="section-recent">
        <h2 class="panel-header">◆ Recently, I:</h2>
        <ul class="highlight-list">
          {highlights.data.what_makes_me_different.map((item: any) => {
            const mainPoint = typeof item === 'string' ? item : (item.point || '');
            const subPoints: string[] = typeof item === 'string' ? [] : (item.details || []);
            const parts = mainPoint.split(/\[LOGO:([^\]]+)\]/g);
            return (
              <li class="highlight-item-group">
                <div class="flex items-start">
                  <span class="bullet-point"></span>
                  <div class="flex-1">
                    <div class="highlight-main-point">
                      {parts.map((part: string, i: number) => {
                        if (i % 2 === 1) {
                          const n = resolveLogoFileName(part);
                          return <img src={`/logos/${n}.png`} alt={part} width="20" height="20" loading="lazy" class="inline-block align-middle mx-1 logo-placeholder" onerror={(e) => {
                            const img = e.target as HTMLImageElement;
                            if (!img.dataset.fallback) {
                              img.dataset.fallback = '1';
                              img.src = `/logos/${n}.svg`;
                            } else {
                              img.style.display = 'none';
                            }
                          }} />;
                        }
                        return <span set:html={formatStatsAndCompanies(part)}></span>;
                      })}
                    </div>
                    {subPoints.length > 0 && (
                      <ul class="highlight-sub-points">
                        {subPoints.map((sub: string) => (
                          <li class="highlight-sub-item" set:html={formatStatsAndCompanies(sub)}></li>
                        ))}
                      </ul>
                    )}
                  </div>
                </div>
              </li>
            );
          })}
        </ul>
      </div>
    )}

    {highlights?.data?.building?.length > 0 && (
      <div class="scroll-stack-card" id="section-building">
        <h2 class="panel-header">◆ Things in progress:</h2>
        <ul class="highlight-list">
          {highlights.data.building.map((item: any) => {
            const mainPoint = typeof item === 'string' ? item : (item.point || '');
            let subPoints: string[] = typeof item === 'string' ? [] : (item.details || []);
            if (mainPoint.includes('TechPlus UW')) {
              subPoints = ["Organised mentorship & dinner for <strong><em>200+</em></strong> members for the F25 term.", ...subPoints];
            }
            const parts = mainPoint.split(/\[LOGO:([^\]]+)\]/g);
            return (
              <li class="highlight-item-group">
                <div class="flex items-start">
                  <span class="bullet-point"></span>
                  <div class="flex-1">
                    <div class="highlight-main-point">
                      {parts.map((part: string, i: number) => {
                        if (i % 2 === 1) {
                          const n = resolveLogoFileName(part);
                          return <img src={`/logos/${n}.png`} alt={part} width="20" height="20" loading="lazy" class="inline-block align-middle mx-1 logo-placeholder" onerror={(e) => {
                            const img = e.target as HTMLImageElement;
                            if (!img.dataset.fallback) {
                              img.dataset.fallback = '1';
                              img.src = `/logos/${n}.svg`;
                            } else {
                              img.style.display = 'none';
                            }
                          }} />;
                        }
                        return <span set:html={formatStatsAndCompanies(part)}></span>;
                      })}
                    </div>
                    {subPoints.length > 0 && (
                      <ul class="highlight-sub-points">
                        {subPoints.map((sub: string) => (
                          <li class="highlight-sub-item" set:html={formatStatsAndCompanies(sub)}></li>
                        ))}
                      </ul>
                    )}
                  </div>
                </div>
              </li>
            );
          })}
        </ul>
      </div>
    )}

    {testimonials.length > 0 && (
      <div class="scroll-stack-card" id="section-clients">
        <p class="panel-header">◆ Client References</p>
        <div class="quotes-list">
          {testimonials.map((t) => (
            <div class="quote-block">
              {t.data.project && <p class="quote-project">{t.data.project}</p>}
              <p class="quote-text">"{t.data.text}"</p>
              <p class="quote-author">— {t.data.author}, {t.data.title} · {t.data.company}</p>
            </div>
          ))}
        </div>
      </div>
    )}

  </ScrollStack>

  <!-- ── LET'S CONNECT — standalone footer section ─────────────────────────── -->
  <section id="section-connect1" class="connect-section">
    <p class="contact-eyebrow">◆ Email</p>
    <h2 class="contact-heading">Let's connect.</h2>
    <p class="contact-body">
      Reach me directly at
      <a href="mailto:elrichchen123@gmail.com" class="contact-inline-link">elrichchen123@gmail.com</a>.
    </p>
    <a href="mailto:elrichchen123@gmail.com" class="contact-cta">Send an email</a>
    <div class="contact-icons">
      {contactLinks.map((link) => (
        <a
          href={link.href}
          aria-label={link.label}
          class="contact-icon"
          target={link.href.startsWith('mailto:') ? undefined : '_blank'}
          rel={link.href.startsWith('mailto:') ? undefined : 'noopener noreferrer'}
        >
          <span class="contact-icon-svg" set:html={link.icon}></span>
        </a>
      ))}
    </div>
  </section>


</Base>

<style>
  /* ── Scroll sections ─────────────────────────────────────────────────────── */
  .scroll-section {
    width: 100vw;
    position: relative;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    padding: 3rem 0;
    z-index: 5;
  }

  /* ── Hero stays full viewport height ─────────────────────────────────────── */
  .hero-section {
    min-height: calc(100svh - var(--nav-clearance));
    justify-content: center;
    flex-direction: column;
    text-align: center;
    padding: calc(var(--nav-clearance) * 0.22) 0 0;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    user-select: none;
    width: min(100%, calc(100vw - 1.4rem));
    margin-inline: auto;
  }

  /* Allow mouse events through to the name so TextPressure cursor tracking works,
     but disable them on the tagline / scroll-hint decorations */
  .hero-tagline,
  .hero-scroll-hint {
    pointer-events: none;
  }

  .hero-name {
    font-family: 'Syne', sans-serif;
    font-size: clamp(3.5rem, 14vw, 10rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1;
    margin: 0;
    text-align: center;
    text-transform: uppercase;
    color: #ffffff;
    user-select: none;
  }

  .hero-tagline {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.45rem 1.25rem;
    border-radius: 9999px;
    background: rgba(255, 255, 255, 0.82);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.4);
    font-size: clamp(1.1rem, 2.6vw, 1.45rem);
    letter-spacing: 0.13em;
    text-transform: uppercase;
    color: rgb(var(--fg));
    font-weight: 500;
    pointer-events: none;
    white-space: normal;
    flex-wrap: wrap;
    justify-content: center;
    max-width: 100%;
    text-wrap: balance;
  }

  .tagline-link {
    color: rgb(var(--fg));
    font-weight: 700;
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: rgba(var(--fg), 0.35);
    pointer-events: auto;
    transition: text-decoration-color 0.2s ease;
  }

  .tagline-link:hover {
    text-decoration-color: rgba(var(--fg), 0.9);
  }

  .tagline-plain {
    color: rgb(var(--fg));
  }

  .tagline-dot {
    opacity: 0.3;
  }

  :global(.dark) .hero-tagline {
    background: rgba(var(--bg), 0.72);
    border-color: rgba(var(--border), 0.14);
  }

  .hero-scroll-hint {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.3rem;
    margin-top: 2rem;
    opacity: 0.45;
  }

  .hint-label {
    font-size: 0.65rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: #fff;
  }

  .hint-arrow {
    font-size: 1.2rem;
    color: #fff;
    animation: hint-bounce 1.8s ease-in-out infinite;
  }

  @keyframes hint-bounce {
    0%, 100% { transform: translateY(0); }
    50%       { transform: translateY(8px); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-scroll-hint { animation: none; }
  }


  /* ── Panel headers ─────────────────────────────────────────────────────── */
  .panel-header {
    font-size: 1.6rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: rgb(var(--fg));
    margin: 0 0 1.5rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid rgba(var(--border), 0.15);
  }

  /* ── Highlight lists (reuse global.css classes) ─────────────────────── */
  .highlight-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .highlight-item-group { }

  .bullet-point {
    display: inline-block;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: rgb(var(--fg));
    margin-top: 0.5em;
    margin-right: 0.75rem;
    flex-shrink: 0;
    opacity: 0.5;
  }

  .highlight-main-point {
    font-size: 1.35rem;
    line-height: 1.6;
    color: rgb(var(--fg));
  }

  .highlight-sub-points {
    list-style: none;
    margin: 0.4rem 0 0 0.5rem;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    border-left: 2px solid rgba(var(--border), 0.12);
    padding-left: 0.75rem;
  }

  .highlight-sub-item {
    font-size: 1.15rem;
    line-height: 1.6;
    color: rgb(var(--muted));
  }

  /* ── Contact panels ────────────────────────────────────────────────────── */
  .contact-panel { gap: 0; }

  .contact-eyebrow {
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: #fff;
    margin: 0 0 0.75rem;
  }

  .contact-heading {
    font-size: 3rem;
    font-weight: 700;
    margin: 0 0 1rem;
    color: #fff;
  }

  .contact-body {
    font-size: 1.35rem;
    line-height: 1.6;
    color: #fff;
    margin: 0 0 1.5rem;
  }

  .contact-inline-link {
    color: #fff;
    font-weight: 600;
    text-decoration: none;
    border-bottom: 1px solid rgba(var(--border), 0.3);
  }

  .contact-inline-link:hover { border-bottom-color: rgba(var(--border), 0.6); }

  :global(html.light) .contact-eyebrow,
  :global(html.light) .contact-body,
  :global(html.light) .contact-inline-link {
    color: #fff;
  }

  .contact-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.5rem 1.1rem;
    border-radius: 999px;
    border: 1px solid rgba(var(--border), 0.22);
    color: rgb(var(--fg));
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    background: rgba(255, 255, 255, 0.6);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    margin-bottom: 1rem;
  }

  .contact-cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(0,0,0,0.08);
  }

  :global(html.dark) .contact-cta {
    background: rgba(30, 30, 30, 0.7);
    border-color: rgba(255,255,255,0.15);
  }

  .contact-icons {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .contact-icon {
    width: 52px;
    height: 52px;
    border-radius: 14px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: rgb(var(--fg));
    background: rgba(255, 255, 255, 0.75);
    border: 1px solid rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
  }

  .contact-icon:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
    background: rgba(255, 255, 255, 0.95);
  }

  .contact-icon-svg {
    display: inline-flex;
    width: 24px;
    height: 24px;
  }

  .contact-icon svg { width: 100%; height: 100%; }

  :global(html.dark) .contact-icon {
    color: #ffffff;
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.15);
  }

  :global(html.dark) .contact-icon:hover {
    background: rgba(255, 255, 255, 0.16);
    border-color: rgba(255, 255, 255, 0.35);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.35);
  }

  /* ── Standalone connect section ─────────────────────────────────────────── */
  .connect-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 5rem 1.5rem 6rem;
    position: relative;
    z-index: 5;
    --muted: 40 40 40;
  }

  :global(html.dark) .connect-section {
    --muted: 210 210 210;
  }

  .connect-section .contact-eyebrow { margin-bottom: 0.5rem; }
  .connect-section .contact-heading { margin-bottom: 0.75rem; }
  .connect-section .contact-body    { margin-bottom: 1.25rem; }
  .connect-section .contact-icons   { margin-top: 0.25rem; justify-content: center; }

  /* ── Client References ─────────────────────────────────────────────────── */
  /* ── Client references — plain quotes ───────────────────────────────────── */
  .quotes-list {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
    margin-top: 1.25rem;
  }

  .quote-block {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .quote-project {
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgb(var(--muted));
    margin: 0;
  }

  .quote-text {
    font-size: 1.35rem;
    line-height: 1.8;
    color: rgb(var(--fg));
    font-style: italic;
    margin: 0;
  }

  .quote-author {
    font-size: 1.35rem;
    font-weight: 600;
    color: rgb(var(--fg));
    margin: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .hint-arrow { animation: none; }
  }

  @media (max-width: 640px) {
    .panel-left  { margin-left:  1.25rem; }
    .panel-right { margin-right: 1.25rem; }
  }

  @media (max-width: 1024px) {
    .hero-name {
      font-size: clamp(2.65rem, 12.5vw, 6.4rem);
    }

    .hero-tagline {
      padding: 0.45rem 0.9rem;
      font-size: clamp(0.9rem, 2.35vw, 1.2rem);
      letter-spacing: 0.08em;
      row-gap: 0.3rem;
      column-gap: 0.45rem;
    }

    .panel-header {
      font-size: 1.25rem;
      margin-bottom: 1rem;
    }

    .highlight-main-point,
    .quote-text,
    .quote-author {
      font-size: 1.04rem;
      line-height: 1.55;
    }

    .highlight-sub-item,
    .contact-body {
      font-size: 1rem;
      line-height: 1.55;
    }
  }

  @media (max-width: 768px) {
    .hero-tagline {
      row-gap: 0.35rem;
      column-gap: 0.45rem;
      max-width: calc(100vw - 1rem);
    }

    .tagline-dot {
      display: none;
    }
  }

  @media (max-width: 480px) {
    .hero-content {
      gap: 0.55rem;
      width: calc(100vw - 0.75rem);
    }

    .hero-name {
      font-size: clamp(1.95rem, 11.5vw, 3.1rem);
      line-height: 0.96;
      letter-spacing: -0.015em;
    }

    .hero-tagline {
      row-gap: 0.25rem;
      column-gap: 0.35rem;
      padding: 0.38rem 0.56rem;
      font-size: clamp(0.58rem, 2.35vw, 0.69rem);
      letter-spacing: 0.05em;
      line-height: 1.2;
      max-width: calc(100vw - 0.75rem);
    }

    .tagline-dot {
      display: none;
    }

    .tagline-link,
    .tagline-plain {
      white-space: normal;
      overflow-wrap: anywhere;
      text-wrap: balance;
    }

    .panel-header {
      font-size: 1.05rem;
      letter-spacing: 0.04em;
    }

    .highlight-list {
      gap: 1rem;
    }

    .highlight-main-point,
    .quote-text,
    .quote-author {
      font-size: 0.92rem;
      line-height: 1.52;
    }

    .highlight-sub-item {
      font-size: 0.84rem;
      line-height: 1.5;
    }

    .contact-heading {
      font-size: 2rem;
    }

    .contact-body {
      font-size: 0.95rem;
    }

    .connect-section {
      padding: 4rem 1rem 5rem;
    }
  }
</style>
